@PACKAGE_INIT@

# This version
set(HEPCMake_VERSION_MAJOR @HCM_MAJOR_VERSION@)
set(HEPCMake_VERSION_MINOR @HCM_MINOR_VERSION@)
set(HEPCMake_VERSION_PATCH @HCM_PATCH_VERSION@)

set(HCM_FIND_MODULE_DIR "@PACKAGE_FIND_MODULES_INSTALL_DIR@")
set(HCM_MODULE_DIR "@PACKAGE_MODULES_INSTALL_DIR@")
set(HCM_CET_MODULE_DIR "@PACKAGE_CET_MODULES_INSTALL_DIR@")
set(HCM_MODULE_PATH
  "${HCM_MODULE_DIR}"
  "${HCM_FIND_MODULE_DIR}"
  "${HCM_CET_MODULE_DIR}"
  )

